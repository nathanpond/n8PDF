<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <RootNamespace>n8PDF</RootNamespace>

        <!--
          Every input is a file someone else wrote, and the recurring shape of an attack is a
          length, a count or an offset read straight off the wire that overflows on the way to an
          allocation size, an array index or a cast. Checking arithmetic across the assembly turns
          that overflow into a catchable OverflowException at the point it happens, instead of a
          silently wrapped value that sizes a buffer wrong or indexes out of bounds — the whole
          integer-overflow class fails safely rather than corrupts (#266). It reaches the code the
          Semgrep rules cannot (primary constructors its C# grammar skips), and it is the runtime
          twin of the `unchecked-round-cast` and `additive-length-bound-overflow` rules.

          Cheap here on purpose: this affects integer arithmetic and integer casts only, and the
          layout, metric and geometry hot paths are all `double`, which does not participate. The
          two places that MEAN to wrap — the SFNT table checksum and the FNV-1a subset-tag hash —
          say so with an explicit `unchecked` block, which still wins locally.
        -->
        <CheckForOverflowUnderflow>true</CheckForOverflowUnderflow>

        <!--
          The documentation is shipped: what a rule is and where it was measured from is written
          on the types that carry it, and that is worth having in a consumer's editor. What is not
          answered is the compiler's per-member nag — this documents behaviour at the level of a
          type and a rule rather than restating every property's name as a sentence.
        -->
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
        <NoWarn>$(NoWarn);CS1591;CS1573</NoWarn>
    </PropertyGroup>

    <!--
      This project must never gain a PackageReference. n8PDF converts DOCX to PDF entirely from
      scratch on top of the base class library; a third-party dependency here defeats the point.
    -->

    <PropertyGroup Label="Package">
        <PackageId>n8PDF</PackageId>

        <!--
          What a local build gets. A release states its own version on the command line, taken
          from the tag it is cut at, so that the tag is the one place a version is written down.
        -->
        <Version>1.0.0</Version>
        <Authors>Nathan Pond</Authors>
        <Product>n8PDF</Product>
        <Title>n8PDF — DOCX to PDF, from scratch</Title>
        <!-- One line: NuGet shows the description as it is written, whitespace and all. -->
        <Description>Converts .docx to PDF with no third-party DOCX or PDF library, no headless Word or LibreOffice, and no sidecar service: one assembly on top of the base class library. It brings its own OPC reader, WordprocessingML model and style cascade, TrueType and CFF font engine with shaping and bidirectional text, layout engine and PDF writer. Every layout rule in it was measured against Word's own export rather than read out of the specification.</Description>
        <Copyright>Copyright (c) 2026 Nathan Pond</Copyright>
        <PackageLicenseExpression>MIT</PackageLicenseExpression>
        <PackageReadmeFile>PACKAGE.md</PackageReadmeFile>
        <PackageTags>docx;pdf;word;openxml;wordprocessingml;conversion;typesetting;pdf-generation</PackageTags>
        <PackageRequireLicenseAcceptance>false</PackageRequireLicenseAcceptance>

        <!-- Where the package came from, so a consumer can get back to the source of a rule. -->
        <PackageProjectUrl>https://github.com/nathanpond/n8PDF</PackageProjectUrl>
        <RepositoryUrl>https://github.com/nathanpond/n8PDF</RepositoryUrl>
        <RepositoryType>git</RepositoryType>

        <!--
          Symbols go alongside so a stack trace through the converter is readable. They are not
          source-linked: SourceLink is a PackageReference, and this project may not have one. What
          stands in for it is RepositoryCommit, which the release workflow stamps with the commit
          it built — enough to find the source of a frame by hand.
        -->
        <IncludeSymbols>true</IncludeSymbols>
        <SymbolPackageFormat>snupkg</SymbolPackageFormat>

        <!-- The same input builds the same assembly, whoever builds it. -->
        <Deterministic>true</Deterministic>

    </PropertyGroup>

    <ItemGroup>
        <None Include="PACKAGE.md" Pack="true" PackagePath="/" />
        <None Include="../../LICENSE" Pack="true" PackagePath="/" Visible="false" />
    </ItemGroup>

    <ItemGroup>
        <InternalsVisibleTo Include="n8PDF.Tests" />
        <InternalsVisibleTo Include="n8PDF.Fuzz" />
    </ItemGroup>

</Project>
